- Plot loss on all datasets
- Select reasonable data splits
- Figure out the training / validation split
- Find good network structures which work on many data splits
  - Smaller networks as a baseline
  - Batch normalization
  - Dropout
- Normalize input data properly
- Ensure everything works with multiple data sizes
- Find two more datasets to experiment on
- Plot the MSE thing
